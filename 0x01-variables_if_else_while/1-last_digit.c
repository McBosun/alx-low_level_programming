#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *  Main - if the last digit of n is greater than 5
 *  return - always return 0
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n > 5)
		printf("and the last  %d is > than 5\n", n)
			if (n == 0)
				printf("the last digit of n is 0: the %d and is 0\n", n)
					if (n < 6 != 0)
printf("the last digit of n is < than 6 and not 0\n" n)
	return (0);
}
